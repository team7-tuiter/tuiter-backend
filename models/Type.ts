/**
 * An enumeration that defines types of messages a
 * message can have.
 * 
 * @see User
 */
 enum MessageType {
    Image = 'IMAGE',
    String = 'STRING',
    Video = 'VIDEO'
};
export default MessageType;
